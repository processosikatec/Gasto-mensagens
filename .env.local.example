# Digisac — copie para .env.local e preencha
DIGISAC_BASE_URL=https://processosikatec.digisac.io
DIGISAC_TOKEN=cole_o_token_aqui

# Cambio USD->BRL (opcional). Se vazio, o backend busca de uma API publica.
# USD_BRL_RATE=5.40

# Tarifa Meta de override manual (opcional, em USD por mensagem entregue).
# Se vazio, o backend faz scrape + fallback embutido.
# META_SERVICE_RATE_USD=0.0068
